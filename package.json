{
  "name": "glob-demo-app",
  "version": "1.0.0",
  "description": "Simple app that lists JS files using glob's legacy callback API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "glob": "7.2.3"
  }
}
